<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ali-shop</title>

    <!-- 💬 فونت آوسام برای آیکن تلگرام (در صورت نیاز می‌تونی حذف کنی) -->

    <style>
        /* 🎨 متغیرهای رنگ اصلی — فقط کافیست مقدارشان را تغییر دهید */
        :root {
            --main-color: #1e88e5;
            --secondary-color: #64b5f6;
            --background-gradient: linear-gradient(135deg, #e0f7ff 0%, #b3e0ff 50%, #87ceeb 100%);
        }

        /* 🧱 تنظیمات پایه */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: var(--background-gradient);
            color: #333;
            min-height: 100vh;
            padding: 20px;
            position: relative;
            overflow-x: hidden;
        }

        /* 💫 پس‌زمینه شبکه‌ای متحرک */
        .grid-background {
            position: fixed;
            inset: 0;
            z-index: -1;
            opacity: 0.4;
            background:
                linear-gradient(rgba(255, 255, 255, 0.1) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255, 255, 255, 0.1) 1px, transparent 1px);
            background-size: 30px 30px;
            animation: gridMove 20s infinite linear;
        }

        @keyframes gridMove {
            from { background-position: 0 0; }
            to { background-position: 30px 30px; }
        }

        /* 🌌 ذرات متحرک پس‌زمینه */
        .particles {
            position: fixed;
            inset: 0;
            z-index: -1;
            overflow: hidden;
        }
        .particle {
            position: absolute;
            background: rgba(255, 255, 255, 0.5);
            border-radius: 50%;
            animation: float 15s infinite ease-in-out;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) translateX(0); }
            25% { transform: translateY(-20px) translateX(10px); }
            50% { transform: translateY(-35px) translateX(-15px); }
            75% { transform: translateY(-15px) translateX(5px); }
        }

        /* 💬 هدر سایت */
        header {
            text-align: center;
            padding: 30px 0;
            margin-bottom: 40px;
        }

        /* 💬 لوگو یا نام فروشگاه — برای تغییر نام کافیست متن داخل تگ <div class="logo"> را عوض کنید */
        .logo {
            font-size: 3.5rem;
            font-weight: bold;
            background: linear-gradient(45deg, var(--main-color), #0d47a1);
            -webkit-background-clip: text;
            color: transparent;
            text-shadow: 0 0 15px rgba(33, 150, 243, 0.3);
            margin-bottom: 10px;
        }

        /* 💬 شعار زیر نام فروشگاه — قابل تغییر */
        .tagline {
            font-size: 1.2rem;
            color: var(--main-color);
            opacity: 0.9;
        }

        /* 💬 آیکن شناور تلگرام — لینک را داخل href تغییر دهید */
        .telegram-float {
            position: fixed;
            bottom: 25px;
            left: 25px;
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #0088cc, #2aabee);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 30px;
            box-shadow: 0 5px 15px rgba(0, 136, 204, 0.4);
            z-index: 1000;
            transition: all 0.3s ease;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(0, 136, 204, 0.7); }
            70% { box-shadow: 0 0 0 10px rgba(0, 136, 204, 0); }
            100% { box-shadow: 0 0 0 0 rgba(0, 136, 204, 0); }
        }

        /* 🧩 گرید محصولات */
        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            max-width: 1200px;
            margin: 0 auto;
        }

        /* 💬 کارت محصول */
        .product-card {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            overflow: hidden;
            transition: all 0.4s ease;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
        }

        .product-image {
            width: 100%;
            height: 200px;
            background: linear-gradient(45deg, #bbdefb, #90caf9);
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .product-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .product-content {
            padding: 20px;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }

        /* 💬 عنوان محصول — متن را داخل تگ h3 تغییر دهید */
        .product-title {
            font-size: 1.4rem;
            margin-bottom: 10px;
            color: var(--main-color);
        }

        /* 💬 توضیح محصول — متن را در تگ <p class="product-description"> تغییر دهید */
        .product-description {
            color: #546e7a;
            margin-bottom: 20px;
            flex-grow: 1;
        }

        /* 💬 دکمه خرید — لینک را در href تنظیم کنید */
        .buy-btn {
            display: block;
            text-align: center;
            padding: 12px;
            background: linear-gradient(45deg, var(--main-color), var(--secondary-color));
            color: white;
            border-radius: 8px;
            text-decoration: none;
            font-weight: bold;
            transition: 0.3s;
        }

        .buy-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(33, 150, 243, 0.4);
        }

        /* 💬 فوتر پایین صفحه */
        footer {
            text-align: center;
            padding: 30px 0;
            margin-top: 50px;
            color: var(--main-color);
            font-size: 0.9rem;
        }

        /* 📱 واکنش‌گرا برای موبایل */
        @media (max-width: 768px) {
            .products-grid { grid-template-columns: 1fr; }
            .logo { font-size: 2.5rem; }
            .telegram-float {
                bottom: 15px;
                left: 15px;
                width: 50px;
                height: 50px;
                font-size: 25px;
            }
        }
    </style>
</head>

<body>
    <!-- 💬 پس‌زمینه شبکه‌ای -->
    <div class="grid-background"></div>

    <!-- 💬 ذرات متحرک -->
    <div class="particles"></div>

    <!-- 💬 آیکن شناور تلگرام — لینک خود را وارد کنید -->
    <a href="" class="telegram-float">
        <i class="fab fa-telegram-plane"></i>
    </a>

    <!-- 💬 هدر (نام و شعار فروشگاه) -->
    <header>
        <div class="logo">  عمو علیرضا</div>
        <div class="tagline">   کیرم تو پسر هستی</div>
    </header>

    <!-- 💬 لیست محصولات — برای افزودن محصول جدید، از ساختار یکی از کارت‌ها کپی بگیرید -->
    <main class="products-grid">
        <div class="product-card">
            <!-- 💬 برای تغییر عکس محصول، آدرس را در src وارد کنید -->
            <div class="product-image">
                <img src="c:\Users\alireza\Desktop\web_telegram\5.webp" alt="نام محصول">
            </div>
            <div class="product-content">
                <h3 class="product-title">شماره مجازی</h3>
                <p class="product-description">شماره مجازی برای تلگرام </p>
                <a href="" class="buy-btn">خرید محصول</a>
            </div>
        </div>
    </main>

    <!-- 💬 فوتر پایین سایت -->
    <footer>
        <p>© تمامی حقوق برای <strong>عمو علیی</strong> محفوظ است.</p>
        <p>پشتیبانی: <span>@ali_sk17</span></p>
    </footer>

    <script>
        // 💬 اسکریپت ایجاد ذرات متحرک — نیازی به تغییر ندارد
        document.addEventListener('DOMContentLoaded', function() {
            const container = document.querySelector('.particles');
            for (let i = 0; i < 15; i++) {
                const size = Math.random() * 10 + 5;
                const p = document.createElement('div');
                p.classList.add('particle');
                p.style.width = `${size}px`;
                p.style.height = `${size}px`;
                p.style.top = `${Math.random() * 100}%`;
                p.style.left = `${Math.random() * 100}%`;
                p.style.animationDelay = `${Math.random() * 15}s`;
                container.appendChild(p);
            }
        });
    </script>
</body>
</html>
